[tool.poetry]
name = "hong-community-be"
version = "0.1.0"
description = "Hong Community Backend API"
authors = ["Author <sb07907@naver.com>"]

[tool.poetry.dependencies]
python = "^3.9"
fastapi = "^0.109.0"
uvicorn = {extras = ["standard"], version = "^0.27.0"}
pydantic = {extras = ["email"], version = "^2.6.0"}
python-jose = {extras = ["cryptography"], version = "^3.3.0"}
bcrypt = "^4.1.0"
python-dotenv = "^1.0.0"
sqlalchemy = "^2.0.0"
pymysql = "^1.1.0"
itsdangerous = "^2.1.0"
python-multipart = "^0.0.7"
mangum = "^0.17.0"
cryptography = "^42.0.0"

[tool.poetry.group.dev.dependencies]
pytest = "^8.0.0"
httpx = "^0.26.0"
black = "^24.1.0"
isort = "^5.13.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"